import type { Metadata } from "next";
import { product } from "../product";

export const metadata: Metadata = { title: `Privacy — ${product.name}` };

export default function PrivacyPage() {
  return <main className="shell legalPage"><a className="textLink" href="/">← Back to {product.name}</a><div className="eyebrow">REVIEW PROTOTYPE</div><h1>Privacy, plainly.</h1><p className="lede">This notice describes the website you are viewing. The proposed desktop coaching app is not collecting data through this prototype.</p><section><h2>What this demo does</h2><p>All match records, explanations and charts are synthetic. Demo preferences and saved goals live only in page memory and reset on reload. There is no account system, game connection, screenshot capture, AI request or application database.</p><p>The site does not set application cookies or include analytics or advertising trackers. The hosting provider may process ordinary request metadata, such as IP addresses and browser information, to deliver and secure the site. External policy links take you to sites with their own privacy practices.</p></section><section><h2>What the proposed app would do</h2><p>Only explicitly permitted telemetry would be collected, with player controls for recording, optional own-board screenshots and deletion. The intended purpose is completed-match review and coaching history. Automated opponent-board tracking and current augment-offering ingestion are excluded from the initial scope.</p><p>Before any real collection, we must publish the actual retention periods, storage location, deletion process, processor details and handling of any data sent to an AI service. These decisions are not finalized; the switches on this site do not establish a production privacy policy.</p></section><section><h2>Contact</h2>{product.contactEmail ? <p><a className="textLink" href={`mailto:${product.contactEmail}`}>{product.contactEmail}</a></p> : <p>A public developer contact must be added before submission or launch. This prototype currently accepts no data requests or uploads.</p>}</section><p className="legalFoot">{product.name} is an independent concept and is not endorsed by Riot Games. <a href="/#scope">View review scope →</a></p></main>;
}
